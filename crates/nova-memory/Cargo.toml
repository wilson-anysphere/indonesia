[package]
name = "nova-memory"
version.workspace = true
edition = "2021"

[dependencies]
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
sysinfo = "0.30"
tracing = { workspace = true }

[dev-dependencies]
serde_json = "1.0"
