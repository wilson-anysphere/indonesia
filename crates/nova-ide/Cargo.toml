[package]
name = "nova-ide"
version = "0.1.0"
edition = "2021"

[dependencies]
nova-core = { path = "../nova-core" }
once_cell = "1"
regex = "1"
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[lib]
path = "src/lib.rs"
