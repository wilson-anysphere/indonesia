[package]
name = "nova-flow"
version = "0.1.0"
edition = "2021"

[dependencies]
nova-core = { path = "../nova-core" }
nova-hir = { path = "../nova-hir" }
nova-types = { path = "../nova-types" }

[dev-dependencies]

