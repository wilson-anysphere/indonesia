# `target/` is already ignored globally, but keep the fuzz harness self-contained.
target/

# libFuzzer findings.
artifacts/
crashes/

# Fuzz crate lockfile is generated by cargo-fuzz. The workspace lockfile lives at
# the repository root.
Cargo.lock
